[project]
name = "fastapi-wasmer-satcat"
version = "0.1.1"
description = "Add request counter"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.116.1",
    "hypercorn>=0.17.3",
    "uvicorn>=0.35.0",
]
